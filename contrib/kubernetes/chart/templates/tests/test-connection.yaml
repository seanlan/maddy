apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mailcoin.fullname" . }}-test-connection"
  labels:
    {{- include "mailcoin.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mailcoin.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
